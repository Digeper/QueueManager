# ServiceAccount for QueueManager
apiVersion: v1
kind: ServiceAccount
metadata:
  name: queuemanager-sa
  namespace: muzika
  labels:
    app: queuemanager
    app.kubernetes.io/name: queuemanager
    app.kubernetes.io/part-of: muzika-platform
